가상환경 제작하기

# 명령 프롬프트(cmd) 실행하기

%cd venvmacUnityML
# cd해서 가상환경 설치할 파일까지 이동
# venvmacUnityML 안에 ml-agent 파일 넣기

%ls
# 안에 파일이 없어서 아무것도 안뜸

%python3 -m venv venvmlmac

%ls

%source bin/activate
# 가상환경 실행하기

%pip install tensorflow

%pip --version
# 확인 후 업데이트 필요시, pip install --upgrade pip

%python --version

%python3 --version

...